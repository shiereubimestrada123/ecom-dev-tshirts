import React, { Fragment } from 'react';
import AlertPrompt from '../../components/alertprompt/AlertPrompt';

const Home = (props) => {
  return (
    <Fragment>
      <AlertPrompt />
      <h1>home</h1>
    </Fragment>
  );
};

export default Home;
